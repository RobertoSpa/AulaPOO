package br.org.serratec.calculo;

public class CalcularMedia {
	public static int calcular(int num1, int num2) throws ArithmeticException {
		/*
		 * if (num2 == 0) { throw new
		 * ArithmeticException("pc e meio burro, n pode 0 no segundo pae"); } return
		 * (num1 + num2) / 2;
		 */
		return num1 / num2;
	}
}
